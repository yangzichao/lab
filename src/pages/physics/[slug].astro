---
import ExternalRedirectPage from '../../components/redirects/ExternalRedirectPage.astro';
import { getLegacyClassicalPhysicsRedirectPaths } from '../../data/legacy-redirects/physics-lab-redirects';

export function getStaticPaths() {
  return getLegacyClassicalPhysicsRedirectPaths('zh');
}

const { destination } = Astro.props;
---

<ExternalRedirectPage destination={destination} />
