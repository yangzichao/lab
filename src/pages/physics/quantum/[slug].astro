---
import ExternalRedirectPage from '../../../components/redirects/ExternalRedirectPage.astro';
import { getLegacyQuantumPhysicsRedirectPaths } from '../../../data/legacy-redirects/physics-lab-redirects';

export function getStaticPaths() {
  return getLegacyQuantumPhysicsRedirectPaths('zh');
}

const { destination } = Astro.props;
---

<ExternalRedirectPage destination={destination} />
